Rectangle bounds = scene.getBounds(); // Assuming 'getBounds()' method retrieves scene bounds

// Check if a player is within the scene bounds
if (bounds.contains(player.getPosition())) {
    // Handle player movement or action within bounds
}
